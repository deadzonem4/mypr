 // hide/show top-menu
 $(document).ready(function(){

   $(window).resize(function() {
  if($(window).width() >= 768) { 

    $('.menu-main-menu-container').css('display','block');
  } });
   $(window).resize(function() {  
    if($(window).width() < 768) { 

      $('.menu-main-menu-container').css('display','none');
  } });

});

/* form */
 $(document).ready(function(){
    var pu_FlatBedrooms = $('.free-quote select.pu-fbedrooms');
    var pu_HouseBedrooms = $('.free-quote select.pu-hbedrooms');
    var do_FlatBedrooms = $('.free-quote select.do-fbedrooms');
    var do_HouseBedrooms = $('.free-quote select.do-hbedrooms');
    var submit = $('.free-quote .wpcf7-submit');

    if( $('.pu-propertyType').val() === 'Flat'){
      $('.pu-flabel').show();
      $('.pu-hlabel').hide();
      pu_FlatBedrooms.prop( "disabled", false );
      pu_HouseBedrooms.prop('disabled',true);
    }
    else if($('.pu-propertyType').val() === 'House'){
      $('.pu-flabel').hide();
      $('.pu-hlabel').show();
      pu_HouseBedrooms.prop( "disabled", false );
      pu_FlatBedrooms.prop('disabled',true);
    }
    else{
      $('.pu-flabel').hide();
      $('.pu-hlabel').hide();
      pu_FlatBedrooms.prop('disabled',true);
      pu_HouseBedrooms.prop('disabled',true);
    }
    if( $('.do-propertyType').val() === 'Flat'){
      $('.do-flabel').show();
      $('.do-hlabel').hide();
      do_FlatBedrooms.prop( "disabled", false );
      do_HouseBedrooms.prop('disabled',true);
    }
    else if($('.do-propertyType').val() === 'House'){
      $('.do-flabel').hide();
      $('.do-hlabel').show();
      do_HouseBedrooms.prop( "disabled", false );
      do_FlatBedrooms.prop('disabled',true);
    }
    else{
      $('.do-flabel').hide();
      $('.do-hlabel').hide();
      do_FlatBedrooms.prop('disabled',true);
      do_HouseBedrooms.prop('disabled',true);
    }

    $('.pu-propertyType').change(function() {
      if ($(this).val() === 'Flat') {
        $('.pu-flabel').show();
        $('.pu-hlabel').hide();
        pu_FlatBedrooms.prop( 'disabled', false );
        pu_HouseBedrooms.prop('disabled',true);
 
      }
      else if ($(this).val() === 'House'){
        $('.pu-flabel').hide();
        $('.pu-hlabel').show();
        pu_FlatBedrooms.prop( 'disabled', true );
        pu_HouseBedrooms.prop('disabled',false);

      }
      else {
        $('.pu-flabel').hide();
        $('.pu-hlabel').hide();
        pu_HouseBedrooms.prop('disabled',true);
        pu_FlatBedrooms.prop('disabled',true);
      }
    });

    $('.do-propertyType').change(function() {
      if ($(this).val() === 'Flat') {
        $('.do-flabel').show();
        $('.do-hlabel').hide();
        do_FlatBedrooms.prop( 'disabled', false );
        do_HouseBedrooms.prop('disabled',true);

      }
      else if ($(this).val() === 'House'){
        $('.do-flabel').hide();
        $('.do-hlabel').show();
        do_FlatBedrooms.prop( 'disabled', true );
        do_HouseBedrooms.prop('disabled',false);

      }
      else {
        $('.do-flabel').hide();
        $('.do-hlabel').hide();
        do_HouseBedrooms.prop('disabled',true);
        do_FlatBedrooms.prop('disabled',true);
      }
    });
  });

/*--------- dropdown menu ------*/
$(document).ready(function(){

  $("#menu-item-23>a").removeAttr("href");

  $('#menu-item-23>a').click(function(event){
   event.stopPropagation();
    if ($('#menu-item-23 ul').css('display')== 'block'){
      $('#menu-item-23 ul').hide();
    }
    else{
      $('#menu-item-23 ul').show(200);
    }
   });


  $(window).click(function() {
    if ($('#menu-item-23 ul').css('display')== 'block'){
      $('#menu-item-23 ul').hide(200);
    }
  });

  $('#menu-item-23 ul').click(function(event){
    event.stopPropagation();
  });


  $('.hamburger').click(function(){
    if ($('.menu-main-menu-container').css('display')== 'block'){
      $('.menu-main-menu-container').hide(200);
    }
    else{
      $('.menu-main-menu-container').show(200);
    }
  });
});
/* -------- clicked menus styles --------*/
 $(document).ready(function(){
    $( "#menu-main-menu>li>a" ).each(function() {
      if( $(this).attr("href") == window.location.href){
        $(this).css({"border-bottom": "2px solid #FFF", "text-decoration":"none"});    
      }
    });
  });



 $(document).ready(function(){
    $( ".pages-menu #menu-main-menu>li>a" ).each(function() {
      if( $(this).attr("href") == window.location.href){
        $(this).css({"border-bottom": "2px solid #000", "text-decoration":"none"});    
      }
    });
  });

